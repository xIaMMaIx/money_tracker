Screen Short </br>

 <img width="1421" height="1189" alt="Screenshot 2025-12-25 222815" src="https://github.com/user-attachments/assets/765df7a2-fcc8-4b0b-a76f-24ab80f9170a" />

## 🛠️ การติดตั้งและเริ่มใช้งาน (Installation)

ทำตามขั้นตอนด้านล่างทีละข้อเพื่อติดตั้งและรันโปรแกรมบนเครื่องของคุณ

### 1. Clone โปรเจกต์ (Clone Repository)
เปิด Terminal หรือ Command Prompt แล้วพิมพ์คำสั่งเพื่อดึงซอร์สโค้ดลงมาที่เครื่อง
```bash
git clone https://github.com/xIaMMaIx/money_tracker.git
cd money-tracker

2. สร้างจำลองสภาพแวดล้อม (Virtual Environment)
แนะนำให้ทำขั้นตอนนี้เพื่อแยกไลบรารีของโปรเจกต์ออกจากระบบหลักของคอมพิวเตอร์

สำหรับ Windows:

Bash

python -m venv venv
.\venv\Scripts\activate
สำหรับ macOS / Linux:

Bash

python3 -m venv venv
source venv/bin/activate
3. ติดตั้งไลบรารีที่จำเป็น (Install Dependencies)
ติดตั้งไลบรารีทั้งหมดที่ระบุไว้ใน requirements.txt ด้วยคำสั่ง:

Bash

pip install -r requirements.txt
หมายเหตุ: หากคุณยังไม่ได้สร้างไฟล์ requirements.txt ให้สร้างไฟล์ใหม่แล้วนำรายชื่อด้านบนไปวาง หรือใช้คำสั่ง pip install flet SpeechRecognition pyaudio pythainlp gspread oauth2client แทนได้

4. การตั้งค่าเพิ่มเติม (Optional Configuration)
4.1 การตั้งค่าระบบ Cloud (Google Sheets) หากต้องการสำรองข้อมูล (Backup) ขึ้น Google Sheets:

ไปที่ Google Cloud Console และสร้าง Service Account

ดาวน์โหลดไฟล์ JSON Key มาเก็บไว้ในเครื่อง

เปิดไฟล์ Google Sheet ที่ต้องการเชื่อมต่อ และแชร์สิทธิ์ (Share) ให้กับอีเมลของ Service Account (ที่ลงท้ายด้วย @...iam.gserviceaccount.com)

เมื่อเปิดโปรแกรม ไปที่เมนู Settings > Cloud Sync เลือกไฟล์ JSON และระบุชื่อ Sheet

4.2 ปัญหาเกี่ยวกับ PyAudio (หากติดตั้งไม่ผ่าน) หากเกิด Error ตอนติดตั้ง pyaudio:

Windows: อาจต้องติดตั้ง Microsoft Visual C++ Build Tools ก่อน

macOS: ให้ติดตั้ง PortAudio ก่อนด้วยคำสั่ง brew install portaudio

5. รันโปรแกรม (Run Application)
พิมพ์คำสั่งเพื่อเปิดโปรแกรม:

Bash

python main.py
📦 การสร้างไฟล์ Executable (.exe / .app)
หากต้องการแพ็คโปรแกรมเป็นไฟล์เดียวเพื่อนำไปใช้กับเครื่องอื่นโดยไม่ต้องลง Python:

ติดตั้ง PyInstaller:

Bash

pip install pyinstaller
สร้างไฟล์:

Bash

pyinstaller --name "MoneyTracker" --windowed --onefile --icon=assets/icon.ico main.py
(หากไม่มีไฟล์ icon ให้ลบคำสั่ง --icon=... ออก)

ไฟล์ที่ได้จะอยู่ในโฟลเดอร์ dist/
